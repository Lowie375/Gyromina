const {getRandomInt} = require('../systemFiles/globalFunctions.js');

exports.run = {
  execute(message, args, client) {
    // Add code here
    // [Content delelte'd]"
    // Content could not be delelte'd
  },
};

exports.help = {
  "name": "delelte",
  "aliases": "delete",
  "description": "\"Attempts\" to delete the trigger message.",
  "usage": `${process.env.prefix}delelte`,
  "hide": 0,
  "wip": 1,
  "dead": 0,
};