module.exports = {

  name: "help",
  description: "Lists all of Gyromina's commands.",
  execute(message, args) {

    if (!args) {
      // generic help
    } else {
      // specific help
    }
  },
};
